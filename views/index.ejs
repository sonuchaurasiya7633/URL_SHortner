<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortner Project</title>
    <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background: linear-gradient(135deg, #ff6ec4, #7873f5, #4ADEDE, #C850C0);
      background-size: 400% 400%;
      animation: backgroundShift 15s ease infinite;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    @keyframes backgroundShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .container {
      background: rgba(255, 255, 255, 0.95);
      padding: 3rem 2rem;
      border-radius: 20px;
      max-width: 420px;
      width: 90%;
      background: rgba(0, 0, 0, 0.85);
      backdrop-filter: blur(5px);
      animation: fadeInUp 1s ease forwards;
      transform: translateY(20px);
      opacity: 0;
    }

    @keyframes fadeInUp {
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    h1 {
      font-size: 2.2rem;
      color: #6a0dad;
      margin-bottom: 1.8rem;
      letter-spacing: 1px;
      text-align: center;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    label {
      font-weight: 600;
      font-size: 1rem;
      color: #02fb02;
      text-align: center;
    }

    input[type="text"] {
      padding: 0.8rem 1rem;
      border: 2px solid #d6d6f5;
      border-radius: 10px;
      font-size: 1rem;
      transition: 0.3s ease;
      background: #f9f9ff;
      text-align: center;
    }

    input[type="text"]:focus {
      border-color: #9b5de5;
      box-shadow: 0 0 10px #c77dff99;
    }

    button[type="submit"] {
      padding: 0.8rem 1rem;
      background: linear-gradient(to right, #9b5de5, #f15bb5);
      color: #fff;
      border: none;
      border-radius: 10px;
      font-size: 1.1rem;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s ease;
      box-shadow: 0 4px 15px rgba(155, 93, 229, 0.4);
    }

    button[type="submit"]:hover {
      background: linear-gradient(to right, #f15bb5, #fee440);
      transform: translateY(-3px) scale(1.03);
      box-shadow: 0 6px 20px rgba(255, 105, 180, 0.4);
    }

    p {
      margin-top: 1.2rem;
      font-size: 1rem;
      color: #222;
      word-break: break-word;
    }

    a {
      color: #9b5de5;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.2s ease;
    }

    a:hover {
      color: #ff6ec4;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>URL Shortner Project</h1>
    <form action="/short" method="POST">
      <label for="longUrl">Enter long URL:</label>
      <input type="text" id="longUrl" name="longUrl" placeholder="Enter long URL here" required>
      <button type="submit">Shorten</button>
    </form>
    <% if(shortUrl) { %>
      <p>Short URL :- <a href="<%= shortUrl %>" target="_blank">
        <%= shortUrl %>
      </a></p>
    <% } %>
  </div>
</body>